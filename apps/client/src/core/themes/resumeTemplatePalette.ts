import type { PaletteOptions } from "@mui/material";

const resumeTemplatePalette: PaletteOptions = {
    mode:"light",
    background:{
        // default:'#fff'
    }
}

export default resumeTemplatePalette